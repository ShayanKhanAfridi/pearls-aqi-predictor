# ============================================================
# requirements.txt — Pearls AQI Predictor
# Install: pip install -r requirements.txt
# Python 3.11 ONLY — do NOT use 3.12+ (hopsworks incompatible)
#
# Key changes vs old version:
#   - hopsworks upgraded from 3.7.0 → 4.7.5
#   - twofish removed (was pulling in legacy compiled C extension)
#   - deltalake added (prebuilt wheels, no compilation needed)
# ============================================================

# ── Environment / secrets ────────────────────────────────────
python-dotenv>=1.0.0

# ── Feature Store & Model Registry ───────────────────────────
hopsworks==4.7.5
deltalake==0.17.4        # pinned: 0.18+ Rust HDFS writer breaks on GitHub Actions

# ── Data & ML ────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
joblib>=1.3.0

# ── Data Collection (Open-Meteo) ─────────────────────────────
openmeteo-requests>=1.2.0
requests-cache>=1.1.0
retry-requests>=2.0.0

# ── Explainability ────────────────────────────────────────────
shap>=0.44.0

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.18.0

# ── Dashboard ─────────────────────────────────────────────────
streamlit>=1.31.0

# ── Utilities ─────────────────────────────────────────────────
python-dateutil>=2.8.0
pytz>=2023.3